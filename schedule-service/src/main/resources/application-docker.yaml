spring:
  data:
    mongodb:
      uri: "mongodb://mongodb:27017/schedule-service"
      database: "schedule-service"

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
