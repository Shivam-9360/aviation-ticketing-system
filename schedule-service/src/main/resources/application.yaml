spring:
  application:
    name: "schedule-service"
  datasource:
    url: "jdbc:mysql://localhost:3306/schedule-service"
    username: "root"
    password: "root"
  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
server:
  port: 9004

